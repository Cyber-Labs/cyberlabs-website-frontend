{
  "name": "cyberlabs-website",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.6.0",
    "react-dom": "^16.6.0",
    "react-progressive-image": "^0.6.0",
    "react-router-dom": "^4.3.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "./shell.sh && react-scripts start",
    "client": "react-scripts start",
    "build": "react-scripts build",
    "test": "eslint src public && react-scripts test --env=jsdom",
    "test-dev": "eslint src public && react-scripts test --env=jsdom",
    "lint": "eslint src public",
    "eject": "react-scripts eject",
    "newstart":"react-scripts start"
  }
}
